You are working in the nexusforge/ codebase, a synthetic dataset generation pipeline 
for fine-tuning a chat persona. I've already diagnosed the root causes of a quality 
issue and need you to implement the full fix across the codebase. Work through every 
item below, editing all affected files consistently.

CONTEXT: This pipeline generates two kinds of training samples — "grounded" (derived 
strictly from seed_data/raw/*.txt) and "expansion" (self-instruct samples generated by 
asking the teacher model to riff on existing examples). The expansion path currently 
produces generic, off-persona, corporate-sounding content (e.g. VPN setup guides, 
blockchain explainers) because the prompt has no persona constraint, and it makes up 
50% of the dataset. Fix this so expansion samples stay in-voice and in-domain for the 
intended chat persona.

FIX 1 — configs/project.yaml
Add a `persona:` field (multi-line YAML string) describing the target persona's voice, 
tone, and topics, derived from the actual content in seed_data/raw/01.txt, 02.txt, and 
03.txt (a college student into fitness/sports who wants a witty, conversational, 
joke-when-appropriate assistant, no corporate niceties, serious tone for serious topics). 
Read those three files yourself and base the persona description on what's actually in 
them, don't guess.

FIX 2 — prompts/expansion/self_instruct.txt
Rewrite this prompt template to:
  - Accept a {persona} placeholder and require every generated Output to match that 
    voice (conversational, short sentences, humor where natural, no "Objectives / 
    Activities / Assessment" template structure, no corporate boilerplate).
  - Explicitly forbid drifting into unrelated domains not implied by the persona or the 
    existing example instructions (no inventing topics like blockchain, VPN setup, 
    WordPress themes, etc. unless the persona's source material actually covers them).
  - Still take {num_samples} and {examples} placeholders as before, formatted the same 
    way (Instruction: / Output: pairs) so the existing parser in 
    generators/expansion_generator.py keeps working without changes to its parsing logic.

FIX 3 — generators/expansion_generator.py
Update `ExpansionGenerator.generate()` to accept a `persona: str = ""` parameter and 
pass it into `self.prompt_template.format(...)` alongside the existing num_samples and 
examples args.

FIX 4 — pipeline/orchestrator.py
Find the call site(s) where `expansion_gen.generate(...)` is invoked and pass 
`persona=self.project_config.get("persona", "")` so the persona text actually flows 
from config through to the prompt.

FIX 5 — configs/generation.yaml
Lower `expansion_ratio` from 0.5 to 0.2, so grounded (source-derived) samples dominate 
the dataset and expansion samples are a smaller supplementary slice, appropriate for 
persona fine-tuning rather than broad-coverage instruction tuning.

FIX 6 — quality/verifier.py and prompts/verification/grounding_check.txt
Inspect how verification currently runs. Every existing "expansion" sample in 
datasets/exported/dataset.jsonl has `"verified": false`, which suggests verification 
either isn't being run against expansion samples at all, or is running but its result 
isn't being written back to the metadata, or is failing silently. Find out which of 
these is happening and fix it so:
  - Verification actually executes for expansion samples (not just grounded ones), 
    checking that the output matches the required persona voice/tone, not just factual 
    grounding.
  - The real verification result (true/false) is correctly persisted to 
    metadata.verified instead of being hardcoded to False at creation time.
  - Samples that fail verification are either dropped or flagged for regeneration, 
    consistent with how grounded samples are already handled by quality/verifier.py 
    and quality/heuristic_filter.py — check how grounded samples are filtered today and 
    apply the same standard to expansion samples.

FIX 7 — quality/scorer.py
Check whether quality_score is being computed per-sample or hardcoded. Every existing 
sample in the dataset has quality_score: 1.0 regardless of content, which means it's 
not discriminating. Make this score actually reflect something measurable (e.g. 
length/structure heuristics, persona-voice match, or the verifier's confidence) instead 
of being a constant.

After making these changes, do not run a full regeneration — just confirm the code 
compiles/imports cleanly and the prompt template's placeholders match what 
expansion_generator.py passes to .format(). Summarize every file you changed and why.